// NOTE: File retained only as a stub so imports won't break if cached somewhere.
// Safe to delete entirely after verifying no dynamic imports or references remain.
export default function AIChatbot() { return null; }
